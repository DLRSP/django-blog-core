{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"django-blog-core","text":"<p>Reusable multi-blog Django package for editorial articles.</p> <ul> <li>Getting started</li> <li>Hookset</li> <li>HTML sanitize policy</li> <li>Extras</li> </ul>"},{"location":"extras/","title":"Extras","text":"Extra Status Notes <code>markdown</code> implemented <code>markdown</code> package <code>sanitize</code> implemented <code>bleach</code> <code>search</code> stub Index <code>published()</code> only; host picks backend <code>tags</code> stub Soft dep on host tag library <code>social</code> stub Share matrix; secrets in host env <code>images</code> stub Future media / image-primary format <code>events</code> implemented Domain-neutral <code>ArticleEvent</code> timeline, opt-in app <p>Stubs live under <code>blog_core.extras.*</code> for discoverability.</p>"},{"location":"extras/#events","title":"<code>events</code>","text":"<p><code>blog_core.extras.events</code> ships a domain-neutral <code>ArticleEvent</code> model: a timestamped occurrence attached to an <code>Article</code> (<code>article</code>, <code>occurred_at</code>, <code>label</code>, <code>source</code>, <code>source_id</code>, <code>body</code>, <code>metadata</code>). It carries no domain vocabulary of its own, so a host can use it for any per-article timeline (access log, webhook deliveries, audit trail, ...). <code>(source, source_id)</code> is unique, so re-importing the same upstream record is idempotent.</p> <p>The model and its migrations only load when the app is opted in:</p> <pre><code>pip install \"django-blog-core[events]\"\n</code></pre> <pre><code>INSTALLED_APPS = [\n    ...,\n    \"blog_core\",\n    \"blog_core.extras.events\",\n]\n</code></pre> <pre><code>python manage.py migrate\n</code></pre> <pre><code>from blog_core.extras.events.models import ArticleEvent\n\nArticleEvent.objects.create(\n    article=article,\n    source=\"my-importer\",\n    source_id=\"42\",\n    label=\"example-tag\",\n    occurred_at=timestamp,\n)\narticle.events.all()  # chronological timeline, oldest first\n</code></pre>"},{"location":"getting-started/","title":"Getting started","text":""},{"location":"getting-started/#install","title":"Install","text":"<pre><code>pip install \"django-blog-core[markdown,sanitize]\"\n</code></pre>"},{"location":"getting-started/#configure","title":"Configure","text":"<pre><code>INSTALLED_APPS = [..., \"blog_core\"]\n\nBLOG_CORE = {\n    \"PAGINATE_BY\": 12,\n    \"SANITIZE_HTML\": True,\n}\n</code></pre> <pre><code>urlpatterns = [\n    path(\"blog/\", include(\"blog_core.urls\")),\n]\n</code></pre> <pre><code>python manage.py migrate\n</code></pre>"},{"location":"getting-started/#publish-flow","title":"Publish flow","text":"<ol> <li>Create a Blog in admin.</li> <li>Create an Article (<code>draft</code> \u2192 <code>scheduled</code> or <code>published</code>).</li> <li>Cron: <code>python manage.py publish_scheduled_articles</code>.</li> <li>Public list: <code>/blog/&lt;blog-slug&gt;/</code>.</li> <li>Feeds: <code>/blog/&lt;blog-slug&gt;/feed/rss/</code> and <code>.../atom/</code>.</li> </ol> <p><code>Article.objects.published()</code> is the public SSoT.</p>"},{"location":"hookset/","title":"Hookset","text":"<p>Pinax-style customization without forking.</p> <pre><code># myapp/hooks.py\nfrom blog_core.hookset import DefaultBlogHookSet\n\n\nclass BlogHookSet(DefaultBlogHookSet):\n    def article_absolute_url(self, article):\n        return f\"/writing/{article.blog.slug}/{article.slug}/\"\n</code></pre> <pre><code>BLOG_CORE_HOOKSET = \"myapp.hooks.BlogHookSet\"\n# or BLOG_CORE = {\"HOOKSET\": \"myapp.hooks.BlogHookSet\"}\n</code></pre> <p>Override points: <code>blog_absolute_url</code>, <code>article_absolute_url</code>, <code>can_view</code>, <code>can_preview_with_secret</code>, <code>render_body</code>, <code>feed_title</code>, <code>feed_description</code>.</p> <p>Security: do not widen public visibility beyond <code>published()</code> or bypass authz for drafts.</p>"},{"location":"sanitize/","title":"HTML sanitize policy","text":""},{"location":"sanitize/#defaults","title":"Defaults","text":"Setting Default Meaning <code>SANITIZE_HTML</code> <code>True</code> Run output through the sanitize path <code>ALLOW_RAW_HTML</code> <code>False</code> Skip sanitization (XSS risk)"},{"location":"sanitize/#with-sanitize-bleach","title":"With <code>[sanitize]</code> (bleach)","text":"<p>Allow-list in <code>blog_core.render</code>: common block/inline tags, <code>a[href|title|rel]</code>, <code>img[src|alt|\u2026]</code>, <code>http</code>/<code>https</code>/<code>mailto</code> protocols. Scripts and <code>javascript:</code> URLs are stripped.</p>"},{"location":"sanitize/#without-bleach","title":"Without bleach","text":"<p>If <code>SANITIZE_HTML</code> is true but bleach is not installed, HTML is escaped (not passed through). Install <code>django-blog-core[sanitize]</code> for allow-list cleaning of intentional HTML bodies.</p>"},{"location":"sanitize/#markdown","title":"Markdown","text":"<p><code>body_format=markdown</code> requires <code>[markdown]</code>. Output is then sanitized the same way as HTML when <code>SANITIZE_HTML</code> is enabled.</p>"}]}