"""``[social]`` extra — share / publish-network matrix (stub).

Intended contract (future): generic share targets (url, scheduled date, last
error) without embedding network secrets in this package. Secrets stay in the
host environment.
"""

FEATURE = "social"
STATUS = "stub"
