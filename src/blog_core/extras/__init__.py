"""Optional feature extras (stubs for v0.1).

Install extras via pip::

    pip install "django-blog-core[search]"
    pip install "django-blog-core[tags]"
    pip install "django-blog-core[social]"
    pip install "django-blog-core[images]"
    pip install "django-blog-core[markdown,sanitize]"

Each submodule documents the intended contract; soft dependencies are not
imported at package import time.
"""
