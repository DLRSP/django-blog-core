"""``[tags]`` extra — tagging integration (stub).

Intended contract (future): soft dependency on a tag library
(e.g. django-taggit or a host tagging package) attached to Article.
"""

FEATURE = "tags"
STATUS = "stub"
