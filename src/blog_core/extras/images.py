"""``[images]`` extra — media / filer integration (stub).

Intended contract (future): optional image attachments and a ``media`` /
image-primary ``body_format`` refinement. Not required for HTML/Markdown v0.1.
"""

FEATURE = "images"
STATUS = "stub"
